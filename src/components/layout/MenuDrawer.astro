---
import type IProfile from "../../data/types/profile.type";
import profileImage from "../../assets/image/profile.jpg"
import { Image } from "astro:assets";
interface Props{
    profile:IProfile
}
const {profile}:Props = Astro.props;
---
<nav id="sidebar" class="fixed top-0 left-0 flex flex-col items-center justify-center py-5 transition-all duration-500 ease-linear w-[70vw] h-screen xl:hidden bg-white dark:bg-[#282a36] bg-opacity-50 dark:bg-opacity-50 backdrop-blur-md z-50 -translate-x-full gap-4">
    <Image
        src={profileImage}
        alt="profile"
        width={500}
        height={500}
        class="w-[150px] h-[150px] rounded-full shadow-lg shadow-gray-300 dark:shadow-gray-600"
    />
    <ul class="flex flex-col items-center dark:text-white text-lg w-[80%]">
        {profile.items.map((item) => (
            <a href={item.href} target="_self" class="py-1 hover:bg-gray-300 dark:hover:bg-indigo-500 w-full text-center rounded-3xl">
                {item.label}
            </a>
        ))}
    </ul>
    <ul class="flex flex-row flex-wrap items-center justify-center w-[85%] gap-2">
        {profile.icons.map((icon) =>(
            <li class="">
                <a href={icon.href}>{(<icon.label/>)}</a>
            </li>
        ))}
    </div>
</nav>